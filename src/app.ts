// import express from "express";

// const app = express();

// app.get('/', (req, res) => {
    
// });

// app.listen(5000);

const add = (a: number, b: number): number => {
    return a + b;
}

console.log(add(2, 3));